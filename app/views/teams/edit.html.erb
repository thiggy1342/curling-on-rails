<h1>Edit <%= @team.team_name %></h1>

<%= form_with(model: [@league, @team], local: true) do |form| %>
    <p>
      <%= form.label :team_name %><br>
      <%= form.text_field :team_name %><br>
    </p>
    <p>
      <%= form.submit %>
    </p>
<% end %>