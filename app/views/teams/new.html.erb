<h1>Create New Team</h1>

<%= form_with(model: [@league, @league.teams.build], local: true) do |form| %>
    <p>
      <%= form.label :team_name %><br>
      <%= form.text_field :team_name %><br>
    </p>
    <p>
      <%= form.submit %>
    </p>
<% end %>